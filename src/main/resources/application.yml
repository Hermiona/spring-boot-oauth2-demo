spring:
  profiles:
    active: local
  jpa:
    database: postgresql
    show-sql: false
    hibernate:
      ddl-auto:  validate
  flyway:
    enabled: true
    baseline-on-migrate: true
    out-of-order: true